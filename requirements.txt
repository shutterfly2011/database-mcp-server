# Core MCP and async dependencies
mcp>=1.0.0
fastmcp>=0.2.0

# Web API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database drivers
aiosqlite>=0.20.0
asyncpg>=0.29.0
aiomysql>=0.2.0
motor>=3.5.1
dnspython>=2.6.1

# SQLAlchemy async support
sqlalchemy[asyncio]>=2.0.0
greenlet>=3.0.0

# Configuration management
python-dotenv>=1.0.0

# LLM providers for natural-language-to-SQL generation (all optional - only the
# provider selected via LLM_PROVIDER needs to be installed/importable)
anthropic>=0.39.0
openai>=1.54.0
httpx>=0.27.0

# Data validation
pydantic>=2.5.0

# Logging and utilities
structlog>=23.0.0

# Security
cryptography>=41.0.0